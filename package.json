{
  "name": "supermaultd",
  "version": "1.0.0",
  "description": "A tower defense game built with Python and Pygame",
  "scripts": {
    "build": "python build.py",
    "dist": "python build.py",
    "clean": "python -c \"import shutil, os; [shutil.rmtree(d) for d in ['build', 'dist', '__pycache__', 'SupermaulTD_Distribution'] if os.path.exists(d)]; [os.remove(f) for f in ['SupermaulTD.spec'] if os.path.exists(f)]\"",
    "install-deps": "pip install -r requirements.txt",
    "run": "python main.py",
    "test": "python test_build.py",
    "test-build": "npm run dist && npm run test"
  },
  "keywords": ["game", "tower-defense", "pygame", "python"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "pygame": ">=2.5.0",
    "pygame-gui": ">=0.6.9",
    "pyinstaller": ">=5.13.0",
    "pymunk": ">=6.5.0"
  }
}
